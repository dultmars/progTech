/* package hu.nye.wumpusgame.service.game;
import hu.nye.wumpusgame.model.GameBoardVO;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

*/
/* public class GameStateTest {

    @Test
    public void testEqualsAndHashCode() {
        GameBoardVO board1 = new GameBoardVO(/* initialize the game board as needed */);
   /*     GameBoardVO board2 = new GameBoardVO(/* initialize another game board as needed */);

 /*       GameState state1 = new GameState("user1", board1, true);
        GameState state2 = new GameState("user1", board1, true);
        GameState state3 = new GameState("user2", board1, true);
        GameState state4 = new GameState("user1", board2, true);
        GameState state5 = new GameState("user1", board1, false);

        // Test equals method
        assertTrue(state1.equals(state2));
        assertFalse(state1.equals(state3));
        assertFalse(state1.equals(state4));
        assertFalse(state1.equals(state5));

        // Test hashCode method
        assertEquals(state1.hashCode(), state2.hashCode());
    }

    @Test
    public static final class GameStateBuilder {
        private String username;
        private GameBoardVO currentBoard;
        private boolean shouldExit;

        // A build metódus, amely létrehozza és visszaadja a GameState objektumot
        public GameState build() {
            return new GameState(username, currentBoard, shouldExit);
        }

        // A withusername, withcurrentBoard, withshouldExit metódusok továbbra is ugyanazok maradnak
        public GameStateBuilder withusername(String username) {
            this.username = username;
            return this;
        }

        public GameStateBuilder withcurrentBoard(GameBoardVO currentBoard) {
            this.currentBoard = currentBoard;
            return this;
        }

        public GameStateBuilder withshouldExit(boolean shouldExit) {
            this.shouldExit = shouldExit;
            return this;
        }
    }
} */
